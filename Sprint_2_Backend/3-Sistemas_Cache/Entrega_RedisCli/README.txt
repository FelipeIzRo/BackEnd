Este ejercicio no contiene los dockerflies ni docker-compose.yml

Para ejecutarlo abrir el directorio Chat_redis con Visual studio
Abrir una consola y ejecutar el comando:

	docker run --name some-redis -d redis

Esto creara un Docker con redis y usando los comandos Publish y Subscribe 
mencionados en el documento funciona correctamente.
